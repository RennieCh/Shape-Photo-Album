package model;

/**
 * Enumerates the types of shapes available in the model.
 */
public enum ShapeType {
  /** Represents a rectangle shape. */
  RECTANGLE,
  /** Represents an oval shape. */
  OVAL;
  // If more shapes are added in the future, add them as additional enum constants.
}